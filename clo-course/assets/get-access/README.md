# Get Access — screenshot drop folder

Drop your real screenshots here, named **exactly** as below. The page auto-shows each one
the moment the file exists (until then it shows a dashed "Screenshot to add" placeholder).

| Filename | Status | What to capture |
|----------|--------|-----------------|
| ~~`01-license-key.png`~~ | ➖ not used | Removed — the Lemon Squeezy step is text-only (no screenshot needed). |
| `02-claude-code-extension.png` | ✅ done | Reused from the Business X-Ray tab's setup section (the real "search Claude Code → Install" Extensions screenshot). Replace if you want a fresher one. |
| `03-open-panel.png` | ⬜ needed | The Claude Code chat panel open on the right, with the text box at the bottom. |
| `04-install-scope.png` | ⬜ needed | The "where to install" menu, **"Install for you"** highlighted. |
| `05-activate-license.png` | ⬜ needed | `/activate-license` succeeded — the success message + the two install commands it printed. |
| `06-clo-community-installed.png` | ⬜ needed | clo-community installed/enabled (the `/plugins` → Plugins tab is ideal). |
| `07-onboard.png` | ⬜ needed | Claude beginning the `onboard me` interview. |

**Format:** PNG, ~1600px wide is plenty. Crop to the relevant part of the screen. Blur any real license key.

**Where these are used:** `clo-course/get-access.html` (the "Get Access" tab in the course portal).

Full recording script + capture notes: see **`../../GET-ACCESS-ASSETS.md`**.

> Note: the Business X-Ray tab also has matching install screenshots (Draw.io extension, repo-open view)
> if you want to reuse more of them here later. They were extracted from that page's embedded base64.
